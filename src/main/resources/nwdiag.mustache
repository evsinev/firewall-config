nwdiag {

{{{custom}}}

    {{#networks}}
    network "{{name}}" {
        address = "{{address}}/24";

        {{#hosts}}
            {{name}} [address = "{{ip}}"];
        {{/hosts}}
    }
    {{/networks}}
}
